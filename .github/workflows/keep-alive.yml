name: Keep Koyeb Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping API
        run: |
          curl -L https://exceptional-cordie-leonibel-251434e5.koyeb.app/api/service/health